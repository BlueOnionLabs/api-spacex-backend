- In a command terminal execute "docker-compose up" to run the mysql container based on the docker-compose.yml file provided
- Install Pyhon dependencies if required:
  "pip install pandas"
  "pip install sqlalchemy"
  "pip install mysqlclient"
  "pip install haversine"
- In a command terminal run the python script using followin command "python etl.py"